container_commands:
  01-get:
    command: "bash deploy.sh"
  02-install:
    command: "docker run --rm --interactive --tty \ --volume php-app/public:/app \ composer install --ignore-platform-reqs --no-scripts"
