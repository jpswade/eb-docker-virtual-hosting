container_commands:
  01-setup:
    command: "rm -fr php-app && git clone git@bitbucket.org:deaduseful/domain-api.git php-app"
    cwd: "/var/app/ondeck"
    leader_only: true

packages:
    yum:
        epel-release: []
        mod24_ssl: []
        git: []
