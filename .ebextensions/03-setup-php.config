# Set composer folder for this command and update
commands:
   01-installComposer:
      command: curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer

container_commands:
   00-optimize:
      command: "/usr/local/bin/composer.phar install -o -n"